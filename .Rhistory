library(oSCR)
data(rbs)
ls()
oSCR.fit(scrFrame = rbs$scrFrame,ssDF = rbs$ssDF, getStarts = T)
oSCR.fit(scrFrame = rbs$scrFrame,ssDF = rbs$ssDF, model = list(~session, ~session, ~session), getStarts = T)
library(oSCR)
library(oSCR)
oSCR.fit
